{
  "name": "nexchat",
  "version": "0.2.0",
  "private": true,
  "description": "NexChat PWA et base APK Capacitor.",
  "scripts": {
    "serve": "php -S localhost:8080",
    "apk:init": "npm run apk:prepare-web && npx cap add android",
    "apk:sync": "npm run apk:prepare-web && npx cap sync android",
    "apk:open": "npx cap open android",
    "apk:debug": "bash apk/build-debug.sh",
    "apk:prepare-web": "bash scripts/prepare-capacitor-web.sh"
  },
  "devDependencies": {
    "@capacitor/android": "8.4.1",
    "@capacitor/cli": "8.4.1",
    "@capacitor/core": "8.4.1"
  }
}
